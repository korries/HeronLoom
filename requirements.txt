# HeronLoom — runtime dependencies.
#
#   pip install -r requirements.txt
#
# There is nothing else to install: the project runs from this folder.
# See docs/ARCHITECTURE.md#source-layout-and-imports.

# --- core numerics -----------------------------------------------------------
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.4
joblib>=1.3
threadpoolctl>=3.1
numba>=0.59

# --- configuration, I/O, processes -------------------------------------------
PyYAML>=6.0
requests>=2.31
python-dotenv>=1.0
filelock>=3.13
pyarrow>=14.0
pypdf>=4.0
diskcache>=5.6
psutil>=5.9
tqdm>=4.66

# --- logging -----------------------------------------------------------------
concurrent-log-handler>=0.9.28
colorlog>=6.7

# --- embeddings and topic modelling ------------------------------------------
stopwordsiso>=0.6

# --- dimensionality reduction and clustering ---------------------------------
umap-learn>=0.5.6
hdbscan>=0.8.38
pacmap>=0.7.3

# --- 3-D layout --------------------------------------------------------------
# GPL-3.0-only. Provides the native three-dimensional ForceAtlas2 pass used by
# src/fa2_layout.py (dim=3, which is why 1.1 is the floor).
# See THIRD_PARTY_NOTICES.md.
fa2>=1.1

# --- dashboard ---------------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.4
jinja2==3.1.6
markdown>=3.5

# --- Windows-only ------------------------------------------------------------
pywin32>=306 ; sys_platform == 'win32'
pywinpty>=2.0.13 ; sys_platform == 'win32'
